# Interaction Log: 2026-03-05-001

**ISO-8601 Timestamp:** 2026-03-05T15:20:00Z
**User Prompt:**
Find the root cause of the following errors, and put it to documents, no coding
docker compose exec worker npm run start --workspace=@hn-digest/worker -- --enqueue
... [Error: listen EADDRINUSE: address already in use :::3001] ...

**Agent Response Summary:**
Identified that `worker/src/infrastructure/api-server.ts` automatically starts a Hono server on port 3001 upon import. Since `worker/src/index.ts` imports this module at the top level, any secondary process spawned via `docker compose exec` (while the main container process is running) will attempt to bind to the same port and fail with `EADDRINUSE`.

**Documented In:**
- `docs/QA_HISTORY.md` (Technical Rationale)
